import React from 'react';
import { View, Text } from 'react-native';
import LoginOrCreateForm from './LoginOrSignUpForm';

const Login = () => {
    return (
        <View>
            <LoginOrCreateForm />
        </View>
    );
};

export default Login;
